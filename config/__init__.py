"""Marker so the shipped default YAML config is packaged into the wheel.

Not imported at runtime - `common.seed_config()` copies these files into the
user's config directory on an installed first run.
"""
